/** Shared input/textarea/select styling for the loop steps (matches the rest of the app's forms). */
export const FIELD = "w-full rounded-sm border border-line bg-surface-sunken px-3 py-2 text-text";
